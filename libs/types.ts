export interface UserTypes {
  linkIcon?: JSX.Element;
  linkText: string;
  link: string;
  id: string | number;
}

export interface SingleProduct {
  productName: string;
  productImage: string;
  price: number;
  _id: any;
}
