export type ProductType = {
  productName: string;
  price: number;
  _id: string;
  productImage: string;
};

export type ProductPropsType = {
  productName: string;
  price: number;
  id: string;
  productImage: string;
};
